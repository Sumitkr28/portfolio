{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Sumit Kumar — AI / ML Engineer Portfolio",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for static site'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  }
}
